# 개선사항 2025-10-13

## 📋 요약

네이버 지도 크롤링 시스템의 확장성과 안정성을 대폭 개선했습니다.

---

## 🎯 주요 개선 사항

### 1. 지역 범위 확장 (서울 전역 크롤링)

**변경 전:**

- 단일 위치만 크롤링

**변경 후:**

- 서울 25개 구 전체 크롤링 가능
- 각 구의 중심 좌표 정보 포함

**추가된 구역:**
강남구, 강동구, 강북구, 강서구, 관악구, 광진구, 구로구, 금천구, 노원구, 도봉구, 동대문구, 동작구, 마포구, 서대문구, 서초구, 성동구, 성북구, 송파구, 양천구, 영등포구, 용산구, 은평구, 종로구, 중구, 중랑구

---

### 2. 검색어 체계화 (카테고리별 검색어 관리)

**추가된 카테고리 및 검색어:**

#### 🏨 숙박 (7개 검색어)

- 반려동물 동반 호텔
- 반려동물 동반 펜션
- 애견 호텔
- 애견 펜션
- 반려견 캠핑장
- 반려견 글램핑
- 애견 전용 숙소

#### ☕ 음식점\_카페 (5개 검색어)

- 반려동물 동반 카페
- 반려동물 동반 식당
- 애견 카페
- 애견 레스토랑
- 애견 파티룸

#### 🎾 여가\_산책 (7개 검색어)

- 애견 공원
- 반려동물 공원
- 애견 놀이터
- 반려견 산책로
- 반려견 운동장
- 애견 수영장
- 애견 스파

#### 🛍️ 쇼핑\_생활 (7개 검색어)

- 애견 용품점
- 반려동물 용품점
- 애견 미용실
- 애견 스타일링
- 애견 유치원
- 반려동물 돌봄
- 애견 카페샵

#### 🏥 의료\_헬스케어 (4개 검색어)

- 동물병원
- 애견 한방병원
- 반려동물 재활센터
- 애견 훈련소

#### 🚗 교통\_기타 (5개 검색어)

- 반려동물 동반 캠핑카
- 반려동물 택시
- 펫택시
- 반려동물 장례식장
- 반려동물 카페촌

**총 35개 검색어**

---

### 3. 설정 파일 구조화 (`naver_map_url.json`)

**새로운 JSON 구조:**

```json
{
  "base_url": "https://pcmap.place.naver.com/place/list",
  "url_template": "https://pcmap.place.naver.com/place/list?query={query}&x={x}&y={y}&clientX={x}&clientY={y}&display={display}&ts={ts}&additionalHeight={additionalHeight}&locale={locale}",
  "locations": [
    {"name": "강남구", "x": "127.047", "y": "37.517"},
    ...
  ],
  "queries": {
    "숙박": [...],
    "음식점_카페": [...],
    ...
  },
  "params": {
    "display": "70",
    "ts": "1744774586148",
    "additionalHeight": "76",
    "locale": "ko"
  }
}
```

**장점:**

- URL 하드코딩 제거
- JSON 파일만 수정하면 크롤링 대상 변경 가능
- 유지보수성 향상

---

### 4. 자동 URL 생성 기능

**추가된 함수:**

```python
load_url_config()      # JSON 파일에서 설정 로드
generate_urls(config)  # 모든 조합의 URL 자동 생성
```

**생성되는 URL 수:**

- 25개 구 × 35개 검색어 = **875개 URL**

**결과:**

- 수동 URL 작성 불필요
- 확장성 대폭 향상
- 실수 가능성 제거

---

### 5. 이미지 다운로드 기능 추가

**새로운 기능:**

- 각 장소의 대표 이미지를 자동으로 다운로드
- `tmp/images` 폴더에 저장
- 파일명: `{장소명}_{URL해시}.jpg` (예: `플레이독애견풀빌라_a1b2c3d4.jpg`)

**추가된 라이브러리:**

```python
import requests  # HTTP 요청
import hashlib   # 파일명 해시 생성
```

**구현 내용:**

- 이미지 요소 찾기 (다중 선택자 지원)
- 이미지 URL 추출
- HTTP 다운로드 및 저장
- 에러 처리

**데이터 구조에 추가된 필드:**

```json
{
  "placeName": "장소명",
  "category": "카테고리",
  "address": "주소",
  "location": "강남구",
  "searchCategory": "숙박",
  "searchQuery": "애견 호텔",
  "imageFile": "플레이독애견풀빌라_a1b2c3d4.jpg" // ← 새로 추가
}
```

---

### 6. Stale Element Reference 에러 해결

**문제:**

```
Message: stale element reference: stale element not found
```

**원인:**

- 페이지 버튼을 한번에 모두 찾은 후
- 클릭 시 DOM이 재렌더링되어 나머지 요소들이 무효화됨

**해결 방법:**

#### Before (문제가 있던 코드):

```python
page_a_list = driver.find_elements(...)  # 모든 페이지 버튼 찾기
for page_a in page_a_list:
    page_a.click()  # Stale Element 발생!
```

#### After (개선된 코드):

```python
page_count = len(driver.find_elements(...))  # 개수만 저장
for page_idx in range(page_count):
    if page_idx > 0:
        # 매번 새로 찾아서 클릭
        page_buttons = driver.find_elements(...)
        page_buttons[page_idx].click()
```

**추가 개선:**

- 주소 클릭 시 재시도 로직 추가
- 대기 시간 최적화 (페이지 전환: 1.5초, 클릭 전: 0.3초)
- 에러 발생 시 상세한 로그 출력

---

## 📊 성능 및 확장성

### 크롤링 범위

- **이전:** 1개 URL (단일 위치, 단일 검색어)
- **현재:** 875개 URL (25개 구 × 35개 검색어)
- **개선율:** 87,500% 증가

### 수집 데이터

- **기본 정보:** 장소명, 카테고리, 주소
- **위치 정보:** 검색한 구 정보
- **검색 정보:** 검색 카테고리, 검색어
- **이미지:** 대표 이미지 파일 (새로 추가)

### 안정성

- Stale Element 에러 해결로 안정성 향상
- 재시도 로직으로 일시적 오류 대응
- 상세한 로그로 디버깅 용이

---

## 🔄 코드 구조 개선

### 새로 추가된 함수

1. **`load_url_config()`**

   - JSON 파일에서 URL 설정 로드

2. **`generate_urls(config)`**

   - 모든 위치×검색어 조합의 URL 자동 생성

3. **`download_image(image_url, place_name)`**

   - 이미지 다운로드 및 저장

4. **`crawl_single_page(url_info)`**
   - 단일 URL 크롤링 로직 분리

### 수정된 함수

1. **`init()`**

   - `tmp/images` 디렉토리 자동 생성

2. **`crawl()`**
   - 여러 URL 순회 크롤링
   - 진행률 표시
   - 통계 정보 출력

---

## 📝 파일 구조

```
stroll/crawl/
├── naver_map_url.json    # 크롤링 설정 (새로 추가)
├── crawl.py              # 크롤링 메인 로직 (대폭 개선)
├── process_place.py      # 장소 처리
└── tmp/
    ├── images/           # 이미지 저장 폴더 (새로 추가)
    ├── place_obj_list.ndjson
    └── debug_*.png/html
```

---

## 🎯 향후 개선 가능 사항

1. **병렬 처리**

   - 여러 URL을 동시에 크롤링하여 속도 향상
   - Airflow Dynamic Task Mapping 활용

2. **중복 제거**

   - 여러 검색어에서 같은 장소가 나올 경우 중복 제거

3. **증분 업데이트**

   - 이미 수집한 장소는 스킵
   - 변경사항만 업데이트

4. **지역 확장**

   - 서울 외 다른 도시로 확장
   - 전국 단위 크롤링

5. **이미지 최적화**
   - 이미지 리사이징
   - WebP 포맷 변환으로 용량 절감

---

## 🐛 해결된 버그

1. **Stale Element Reference 에러**

   - 페이지 전환 시 요소 참조 무효화 문제 해결

2. **하드코딩된 URL**

   - 설정 파일로 분리하여 유지보수성 향상

3. **단일 위치 제한**
   - 여러 위치 크롤링 가능하도록 개선

---

## ✅ 테스트 필요 사항

1. 875개 URL 전체 크롤링 테스트
2. 이미지 다운로드 성공률 확인
3. 메모리 사용량 모니터링
4. 네이버 지도 rate limit 확인
5. 장시간 실행 시 안정성 검증

---

## 📚 관련 파일

- `stroll/crawl/naver_map_url.json` - 크롤링 설정
- `stroll/crawl/crawl.py` - 크롤링 메인 로직
- `crawl_places_dag.py` - Airflow DAG 정의
- `개선사항20251013.md` - 이 문서

---

**작성일:** 2025년 10월 13일  
**작성자:** AI Assistant  
**버전:** 1.0
